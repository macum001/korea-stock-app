global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: jp-stock-backend
    metrics_path: /metrics
    static_configs:
      - targets:
          - backend:4000
          - realtime-1:4000
          - realtime-2:4000

rule_files:
  - /etc/prometheus/rules.yml
